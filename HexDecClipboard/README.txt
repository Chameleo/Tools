A simple tool that replaces content of windows clipboard with its current value converted to/from hex.
It is expected to be run using a keyboard shortcut. No GUI is displayed.
In most cases it should be able to figure out which way the conversion should be.
If you want to specify that input value is in hex even though it contains only digits, add console argument h.